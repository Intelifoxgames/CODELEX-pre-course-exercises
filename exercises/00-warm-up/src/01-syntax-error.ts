export {};

function addNumbers(a: number, b: number, c: number): number { // Parameters.
    return a + b + c // Sum of parameters.
}

const result = addNumbers(1, 3, 4); // Enter the arguments.
console.log(result); // Expected output: 8
